# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex
DEBUG=false
HOST=0.0.0.0
PORT=5000

# Security
SECURE_COOKIES=true

# Database
# Currently uses local JSON file (data/store.json)
# For production, consider migrating to a database like PostgreSQL
